The eporner embed seems to completely break our UI once it loads. The Ui is completely fine before the embed loads in. Also now we create a settings menu. In the top navbar add a settings button. The button should open the shadcn dialog component which will act as our settings menu panel. The panel will be like notion's settings menu where on the left side we have subsections and on right the content of those sections. The first will be preferences where they can select which sources they want videos from: eporner, sxyporn and both. One section will be for themes where we will give option to switch between popular themes such as catpuccin, nord, ayu dark,tokyo night, dracula. The themes will be shown as skeleton cards with theme cards of their respective colors like the reference screenshot.
